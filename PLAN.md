# Overall Implementation Plan — Task App

## Overview

Real-time multi-user task management app with organizations (groups for family/teams), subtasks, recurring tasks, time tracking, milestones, comments with @mentions, and real-time websocket updates. Backend and frontend developed continuously, interleaved per feature.

## Tech Stack

| Layer | Tech |
|---|---|
| Frontend | Next.js 16.2.9, React 19.2.4, Tailwind v4, Shadcn UI, Zustand, @dnd-kit, react-big-calendar |
| Backend | Fastify 5, Drizzle ORM 0.45, postgres.js, better-auth (org plugin + Google OAuth), ws |
| Shared | `@meu-projeto/types` (Zod schemas + inferred TS types) |
| DB | PostgreSQL (Neon/Supabase) |
| Real-time | ws via @fastify/websocket |

## Backend Directory Structure

```
apps/backend/src/
  index.ts                     # Fastify boot: register plugins, routes, ws, auth
  env.ts                       # Env validation with zod
  db/
    client.ts                  # postgres.js + drizzle instance
    schema/
      auth.ts                  # better-auth tables (user, session, account, verification)
      organization.ts          # better-auth org tables (organization, member, invitation)
      tasks.ts                 # tasks, tags, task_tags, milestones, recurrence_rules
      collaboration.ts         # comments, mentions, time_entries, notifications
      index.ts                 # re-export everything
    relations.ts               # drizzle relations() definitions
    migrations/                # generated by drizzle-kit
  drizzle.config.ts            # drizzle-kit config (dialect: postgresql)
  auth/
    auth.ts                    # better-auth instance (drizzle adapter + org plugin + google oauth)
    session.ts                 # getSession helper for Fastify requests
  middlewares/
    auth.middleware.ts         # requireAuth — validates session, decorates request with user
    org.middleware.ts          # requireOrgMember — validates user is member of :groupId
  controllers/
    auth.controller.ts         # delegates to better-auth handler
    tasks.controller.ts        # CRUD for tasks + subtasks
    recurrence.controller.ts   # set/update recurrence rules
    tags.controller.ts         # CRUD for tags
    milestones.controller.ts   # CRUD for milestones
    time.controller.ts         # log/list time entries
    comments.controller.ts     # CRUD for comments + mentions
    notifications.controller.ts # list/mark-read notifications
  routes/
    auth.routes.ts             # /api/auth/* -> better-auth handler
    tasks.routes.ts            # /api/groups/:groupId/tasks/*
    tags.routes.ts             # /api/groups/:groupId/tags/*
    milestones.routes.ts       # /api/groups/:groupId/milestones/*
    time.routes.ts             # time entries
    comments.routes.ts         # comments
    notifications.routes.ts    # /api/notifications
  services/
    task.service.ts            # task CRUD, subtask nesting, recurrence generation logic
    recurrence.service.ts      # check-overdue + generate-next-occurrence logic
    tag.service.ts
    milestone.service.ts
    time.service.ts            # time entry CRUD + remaining-time calculation
    comment.service.ts         # comment CRUD + mention parsing + notification creation
    notification.service.ts    # list, mark-read, broadcast via ws
    ws.service.ts              # websocket broadcast helpers + client tracking
  ws/
    ws.ts                      # ws route registration + auth via query token
```

## Frontend Directory Structure

```
apps/frontend/
  app/
    (auth)/
      login/page.tsx
      signup/page.tsx
    dashboard/
      layout.tsx              # auth-guarded shell (sidebar + header)
      page.tsx                # dashboard home / overview (minhas tarefas)
      groups/page.tsx         # list/create organizations (planned)
      tasks/
        page.tsx              # task board (list/kanban/calendar views)
        [taskId]/
          page.tsx            # full task detail (comments, subtasks, time entries)
      [groupId]/
        layout.tsx            # org context provider (planned)
        tasks/page.tsx        # task board (planned)
        milestones/page.tsx   # planned
        time-stats/page.tsx   # optional (planned)
        settings/page.tsx     # optional (planned)
    api/
      auth/login/route.ts     # server-side proxy to backend
      auth/signup/route.ts
      auth/logout/route.ts
    page.tsx                  # public landing page
    error.tsx                 # planned
    middleware.ts             # route protection middleware (dynamic session cookie name)
  lib/
    auth-client.ts            # better-auth client init
    api.ts                    # fetch wrapper (planned)
    ws.ts                     # websocket singleton (planned)
    format.ts                 # planned
    constants.ts              # planned
    mention.ts                # planned
  store/
    auth-store.ts             # zustand: current user + session
    org-store.ts              # planned
  components/
    providers.tsx             # wrap app with auth provider
    auth-form.tsx
    landing-header.tsx
    dashboard-sidebar.tsx
    dashboard-header.tsx
    group-switcher.tsx        # planned
    user-menu.tsx             # planned
    notification-badge.tsx    # planned
    notifications/
      notification-dropdown.tsx  # planned
    tasks/
      task-board.tsx          # planned
      task-list.tsx           # planned
      task-card.tsx           # planned
      task-dialog.tsx         # quick preview (title, description, status, timeEstimate)
      task-filters.tsx        # planned
      task-kanban.tsx         # planned
      kanban-column.tsx       # planned
      task-calendar.tsx       # planned
      calendar-event.tsx      # planned
      subtask-list.tsx        # planned
      subtask-add.tsx         # planned
      recurrence-dialog.tsx   # planned
      time-entry-dialog.tsx   # planned
      time-remaining.tsx      # planned
      time-entries-list.tsx   # planned
      tag-picker.tsx          # planned
      comment-thread.tsx      # planned
      comment-input.tsx       # planned
      comment-item.tsx        # planned
    milestones/
      milestone-card.tsx      # planned
      milestone-dialog.tsx    # planned
    settings/
      tag-manager.tsx         # planned
```

## Additional Shadcn Components Needed

| Component | Step | Purpose |
|---|---|---|
| `avatar` | 4 | User/org avatars |
| `label` | 4 | Form labels |
| `separator` | 4 | Visual dividers |
| `sheet` | 5 | Mobile sidebar |
| `tooltip` | 5 | Action hints |
| `scroll-area` | 5 | Scrollable lists |
| `skeleton` | 5 | Loading states |
| `dialog` | 8 | Quick task preview |
| `alert-dialog` | 8 | Delete confirmations |
| `switch` | 8 | Recurrence toggle, dark mode |
| `progress` | 12 | Milestone/time progress bars |

## Utility Functions

| File | Step | Functions |
|---|---|---|
| `lib/auth-client.ts` | 4 | better-auth client init |
| `lib/api.ts` | 7 | `apiGet`, `apiPost`, `apiPatch`, `apiDelete` (typed fetch wrapper) |
| `lib/ws.ts` | 7 | WebSocket singleton (connect, reconnect, dispatch) |
| `lib/format.ts` | 7 | `formatDuration`, `formatDate`, `formatRelativeTime`, `formatTimeRemaining` |
| `lib/constants.ts` | 7 | Priority/status/frequency labels + colors |
| `lib/mention.ts` | 15 | `parseMentions(text)` — extract @usernames |

## All Frontend Pages

```
app/
  page.tsx                      # Step 5 — public landing page
  (auth)/
    login/page.tsx              # Step 4
    signup/page.tsx             # Step 4
  dashboard/
    layout.tsx                  # Step 5 — auth shell
    page.tsx                    # Step 8 — dashboard home/overview (minhas tarefas)
    groups/page.tsx             # Step 6 — list/create orgs
    tasks/
      page.tsx                  # Step 8 — task board (list/kanban/calendar)
      [taskId]/
        page.tsx                # Step 8 — full task detail (comments, subtasks, time entries)
    [groupId]/
      layout.tsx                # Step 8 — org context
      tasks/page.tsx            # Step 8 — task board (list/kanban/calendar)
      milestones/page.tsx       # Step 12
      time-stats/page.tsx       # Step 14 (optional)
      settings/page.tsx         # Step 15 (optional)
  error.tsx                     # Step 18
```

---

## Implementation Steps

### Step 1: Shared Types Package (`packages/types`)

Expand `src/index.ts` with all Zod schemas + inferred TS types:

- `taskSchema`, `createTaskSchema`, `updateTaskSchema`
- `recurrenceSchema`
- `tagSchema`, `milestoneSchema`
- `timeEntrySchema`
- `commentSchema`, `mentionSchema`
- `notificationSchema`
- `wsMessageSchema` (discriminated union: `task:created|updated|deleted`, `comment:created`, `notification:new`)
- Auth-related: `signInSchema`, `signUpSchema`
- Enums as string unions: `priority`, `taskStatus`, `recurrenceFrequency`, `milestoneStatus`, `notificationType`

---

### Step 2: Backend Foundation (Env + DB Schema + Migrations)

**Files to create:**

- `src/env.ts` — zod-validated env config
- `.env`, `.env.example`
- `drizzle.config.ts` — `dialect: 'postgresql'`
- `src/db/client.ts` — postgres.js + drizzle instance
- `src/db/schema/auth.ts` — user, session, account, verification
- `src/db/schema/organization.ts` — organization, member, invitation
- `src/db/schema/tasks.ts` — tasks, recurrence_rules, tags, task_tags, milestones
- `src/db/schema/collaboration.ts` — comments, mentions, time_entries, notifications
- `src/db/schema/index.ts` — re-export
- `src/db/relations.ts` — all `relations()` definitions

**Commands:**

- `drizzle-kit generate` → `drizzle-kit migrate`

**Verify:** migrations run successfully against Neon/Supabase, tables exist.

---

### Step 3: Backend Auth (better-auth + Middlewares)

**Files to create:**

- `src/auth/auth.ts` — better-auth instance (drizzle adapter, org plugin, Google OAuth)
- `src/auth/session.ts` — `getSession(request)` helper
- `src/middlewares/auth.middleware.ts` — validates session, decorates `request.user`
- `src/middlewares/org.middleware.ts` — validates `:groupId` membership, decorates `request.member`
- `src/routes/auth.routes.ts` + `src/controllers/auth.controller.ts` — delegates to better-auth handler

**Verify:** signup, signin, Google OAuth, session validation, 401/403 errors.

---

### Step 4: Frontend Auth (Login/Signup + Route Protection)

**Pages:**

- `app/(auth)/login/page.tsx`
- `app/(auth)/signup/page.tsx`

**Lib:**

- `src/lib/auth-client.ts` — `createAuthClient({ plugins: [organizationClient()] })`

**Root:**

- `middleware.ts` — protect `(app)/*` routes, redirect unauthenticated -> `/login`

**Components:**

- `src/components/auth-form.tsx` — email/password + Google button
- `src/components/providers.tsx` — wrap app with auth provider

**Additional Shadcn:** `avatar`, `label`, `separator`

**Verify:** full auth flow (signup -> login -> session persists -> logout).

---

### Step 5: Landing Page + Dashboard Shell + Auth Store

**Pages:**

- `app/page.tsx` — public landing page (hero, demo mockup, features)
- `app/dashboard/layout.tsx` — auth-guarded shell (sidebar + header)
- `app/dashboard/page.tsx` — dashboard welcome / overview
- `app/dashboard/groups/page.tsx` — list/create organizations (planned)

**Components:**

- `components/landing-header.tsx` — landing page navbar (Home, About, Contact Us + Sign In/Up)
- `components/dashboard-sidebar.tsx` — nav links (Overview, Groups, Tasks, Calendar, Settings) + user info + logout
- `components/dashboard-header.tsx` — top bar with mobile sheet trigger + notification bell
- `components/group-switcher.tsx` — dropdown to switch active org (planned)
- `components/user-menu.tsx` — avatar dropdown (profile, logout) (planned)
- `components/notification-badge.tsx` (planned)

**Store:**

- `store/auth-store.ts` — zustand store: current user, session, loading state, fetchSession, clearSession

**Middleware:**

- `middleware.ts` — dynamic session cookie name (`better-auth.session_token` in dev, `__Secure-better-auth.session_token` in prod); `/` is public (redirects to `/dashboard` if logged in); `/dashboard/*` is protected (redirects to `/login` if not); auth pages redirect to `/dashboard` when authenticated

**Additional Shadcn:** `sheet` (mobile sidebar), `tooltip`, `scroll-area`, `skeleton`

**Verify:** landing page loads publicly, signup/login flow -> redirected to dashboard -> sidebar shows user info -> logout works.

---

### Step 6: Backend Tasks Core (CRUD + Subtasks)

**Schema changes:**
- `tasks.organizationId` → nullable (tasks can be personal or org-scoped)
- `tasks.userId` → new column (FK to users, cascade delete, for personal tasks)

**Files to create:**

- `src/services/task.service.ts` — create, list (with filters), get (with subtasks/tags/time), update, delete, reorder
- `src/controllers/tasks.controller.ts` — thin wrapper (separate handlers for org vs personal tasks)
- `src/routes/tasks.routes.ts` — zod-validated routes (two route sets)

**Route map:**

```
# Organization tasks (requires auth + org membership)
POST   /api/groups/:groupId/tasks            -> create task (with optional parentId for subtask)
GET    /api/groups/:groupId/tasks            -> list tasks (filters: status, priority, assignee, tag, milestone)
GET    /api/groups/:groupId/tasks/:taskId    -> get task (with subtasks, tags, time entries, comments count)
PATCH  /api/groups/:groupId/tasks/:taskId    -> update task
DELETE /api/groups/:groupId/tasks/:taskId    -> delete task
PATCH  /api/groups/:groupId/tasks/:taskId/reorder -> reorder task within status column

# Personal tasks (requires auth only)
POST   /api/tasks                            -> create personal task
GET    /api/tasks                            -> list personal tasks (filters: status, priority, search)
GET    /api/tasks/:taskId                    -> get personal task
PATCH  /api/tasks/:taskId                    -> update personal task
DELETE /api/tasks/:taskId                    -> delete personal task
PATCH  /api/tasks/:taskId/reorder            -> reorder personal task
```

**Verify:** API CRUD works via curl/REST client, subtask creation via `parentId`, personal tasks work without org membership.

---

### Step 7: Frontend API Client + WebSocket Client + Task Store

**Lib:**

- `src/lib/api.ts` — fetch wrapper (baseURL from `NEXT_PUBLIC_API_URL`, `credentials: 'include'`, error handling, typed methods)
- `src/lib/ws.ts` — websocket singleton (connect, reconnect, message dispatch)
- `src/lib/format.ts` — `formatDuration(minutes)`, `formatDate`, `formatRelativeTime`, `formatTimeRemaining(estimate, logged)`
- `src/lib/constants.ts` — priority colors/labels, status labels, frequency labels

**Store:**

- `src/store/tasks-store.ts` — tasks[], filters, add/update/remove/reconcile, optimistic mutations

**Verify:** API wrapper fetches tasks from server, store holds state.

---

### Step 8: Frontend Task List View

**Two-Tier Task View Approach:**

1. **Shadcn Dialog (Quick Preview)** — Click task card → shows Title, Description, Status, TimeEstimate
2. **Dedicated Route** (`/dashboard/tasks/[taskId]`) — Full details: comments, subtasks, time entries, author info

**Pages:**

- `app/dashboard/page.tsx` — dashboard overview with single-row toolbar: description + priority tabs + list/grid toggle
- `app/dashboard/tasks/page.tsx` — task list with single-row toolbar: description + priority tabs + list/grid toggle
- `app/dashboard/tasks/[taskId]/page.tsx` — full task detail page

**Components:**

- `src/components/tasks/task-card.tsx` — priority badge, time estimate (clickable → opens dialog)
- `src/components/tasks/task-list.tsx` — flat/grouped list (supports list + grid view modes)
- `src/components/tasks/task-dialog.tsx` — quick preview dialog (title, description, status, timeEstimate) + link to full detail page

**Additional Shadcn:** `dialog` (quick task preview), `alert-dialog` (delete confirmation), `switch` (recurrence toggle)

**Verify:**
- Click task card → dialog opens with quick preview
- Click "Ver detalhes completos" in dialog → navigates to `/dashboard/tasks/[taskId]`
- Full detail page shows comments, subtasks, time entries, author info

---

### Step 9: Backend + Frontend WebSocket Realtime

**Backend:**

- `src/ws/ws.ts` — ws route registration, auth via query token
- `src/services/ws.service.ts` — client tracking `Map`, `broadcastToOrg()`, `broadcastToUser()`
- Wire into task service: broadcast `task:created|updated|deleted` on mutations

**Frontend:**

- Update `src/lib/ws.ts` — connect with session token, parse events, dispatch to store
- Update `src/store/tasks-store.ts` — reconcile ws events (add/update/remove from store, show toast)

**Verify:** two browser windows, create task in one -> appears in other in realtime.

---

### Step 10: Backend + Frontend Tags

**Backend:**

- `src/services/tag.service.ts`, `src/controllers/tags.controller.ts`, `src/routes/tags.routes.ts`

```
POST   /api/groups/:groupId/tags             -> create tag
GET    /api/groups/:groupId/tags             -> list tags
DELETE /api/groups/:groupId/tags/:tagId       -> delete tag
```

**Frontend:**

- `src/components/tasks/tag-picker.tsx` — multiselect for tags on task dialog
- `src/components/settings/tag-manager.tsx` — create/edit/delete tags with colors
- Update task filters to filter by tag

**Verify:** create tag -> assign to task -> filter by tag -> delete tag.

---

### Step 11: Backend + Frontend Subtask Nesting UI

**Backend:**

- Already handled in Step 6 (`parentId` self-reference). Add `getSubtasks(taskId)` to service.

**Frontend:**

- Update `task-card.tsx` — expand/collapse subtasks, subtask count badge
- `src/components/tasks/subtask-list.tsx` — nested list with indentation lines
- `src/components/tasks/subtask-add.tsx` — quick-add subtask inline

**Verify:** create parent task -> add subtasks -> expand/collapse -> complete subtask -> parent status reflects progress.

---

### Step 12: Backend + Frontend Milestones

**Backend:**

- `src/services/milestone.service.ts`, `src/controllers/milestones.controller.ts`, `src/routes/milestones.routes.ts`

```
POST   /api/groups/:groupId/milestones       -> create milestone
GET    /api/groups/:groupId/milestones       -> list milestones
PATCH  /api/groups/:groupId/milestones/:id   -> update milestone
DELETE /api/groups/:groupId/milestones/:id   -> delete milestone
```

**Frontend:**

- `app/(app)/[groupId]/milestones/page.tsx` — milestones list with progress bars
- `src/components/milestones/milestone-card.tsx` — shows linked tasks count, completion %, due date
- `src/components/milestones/milestone-dialog.tsx` — create/edit form
- Update `task-dialog.tsx` — add milestone selector dropdown
- Update `task-card.tsx` — show milestone badge

**Additional Shadcn:** `progress` (for milestone completion bars)

**Verify:** create milestone -> link tasks -> view progress % -> mark milestone complete.

---

### Step 13: Backend + Frontend Recurring Tasks

**Backend:**

- `src/services/recurrence.service.ts` — `generateOverdueRecurrences(orgId)` called before listing tasks, calculates `nextDueAt` cycle
- `src/controllers/recurrence.controller.ts` — set/update/delete recurrence rule on a task
- `src/routes/tasks.routes.ts` — add `PATCH/DELETE /tasks/:taskId/recurrence`

**Frontend:**

- `src/components/tasks/recurrence-dialog.tsx` — frequency (daily/weekly/monthly), interval, daysOfWeek picker, dayOfMonth, completionWindow (hours), endDate
- Update `task-card.tsx` — recurrence icon badge
- Update `task-dialog.tsx` — recurrence section with `Switch` toggle

**Verify:** set recurrence "every 7 days, 12h to complete" -> wait/rewind -> next occurrence auto-generated.

---

### Step 14: Backend + Frontend Time Tracking

**Backend:**

- `src/services/time.service.ts` — log entry, list entries, delete, `getRemainingTime(taskId)`, `getTimeStats(orgId)`
- `src/controllers/time.controller.ts`, `src/routes/time.routes.ts`

```
POST   /api/groups/:groupId/tasks/:taskId/time   -> log a time entry
GET    /api/groups/:groupId/tasks/:taskId/time   -> list time entries for a task
GET    /api/groups/:groupId/time-stats           -> aggregated time stats (per user, per task)
DELETE /api/groups/:groupId/time/:entryId        -> delete time entry
```

**Frontend:**

- `src/components/tasks/time-entry-dialog.tsx` — log time (start, end, duration, note)
- `src/components/tasks/time-remaining.tsx` — "1 day and 12 hours remaining" display based on `timeEstimateMinutes - sum(entries)`
- `src/components/tasks/time-entries-list.tsx` — list of logged entries on a task
- `app/(app)/[groupId]/time-stats/page.tsx` (optional) — per-user and per-task time dashboard with progress bars
- Update `task-card.tsx` — show time remaining badge if estimate set

**Verify:** set estimate -> log time entries -> see remaining time update -> view stats.

---

### Step 15: Backend + Frontend Comments + Mentions + Notifications

**Backend:**

- `src/services/comment.service.ts` — CRUD, parse `@username` from content, create mentions + notifications
- `src/services/notification.service.ts` — list, mark-read, mark-all-read, broadcast `notification:new` via ws
- `src/controllers/comments.controller.ts`, `src/controllers/notifications.controller.ts`
- `src/routes/comments.routes.ts`, `src/routes/notifications.routes.ts`
- Wire ws broadcast: `comment:created` to org, `notification:new` to user

```
POST   /api/groups/:groupId/tasks/:taskId/comments  -> create comment (parses @mentions)
GET    /api/groups/:groupId/tasks/:taskId/comments  -> list comments
DELETE /api/comments/:commentId                      -> delete comment

GET    /api/notifications                     -> list my notifications
PATCH  /api/notifications/:id/read            -> mark as read
PATCH  /api/notifications/read-all             -> mark all as read
```

**Frontend:**

- `src/components/tasks/comment-thread.tsx` — scrollable thread
- `src/components/tasks/comment-input.tsx` — textarea with `@mention` autocomplete (queries org members)
- `src/components/tasks/comment-item.tsx` — with delete, mentions highlighted
- `src/components/notifications/notification-dropdown.tsx` — dropdown from header bell
- Update `notification-badge.tsx` — unread count, live update via ws
- `app/(app)/[groupId]/settings/page.tsx` (optional) — notification preferences
- `src/lib/mention.ts` — `parseMentions(text)` — extract @usernames

**Verify:** comment on task -> @mention a member -> they get live notification badge -> mark read.

---

### Step 16: Frontend Kanban View

**Components:**

- `src/components/tasks/task-kanban.tsx` — columns: `todo | in_progress | done | archived`, dnd-kit sortable
- `src/components/tasks/kanban-column.tsx` — droppable column, task cards

**Updates:**

- Update `task-board.tsx` — view toggle (List <-> Kanban) using `<Tabs>`
- Drag-to-change-status -> optimistic update -> API PATCH -> ws broadcast

**Verify:** drag task between columns -> status updates everywhere in realtime.

---

### Step 17: Frontend Calendar View (Week + Day)

**Install:** `react-big-calendar` + `@types/react-big-calendar` (handle React 19 peer dep overrides)

**Components:**

- `src/components/tasks/task-calendar.tsx` — wrapped `Calendar` with week + day views
- `src/components/tasks/calendar-event.tsx` — custom event renderer (priority color, title)

**Updates:**

- Filter tasks by dueDate for calendar display
- Update `task-board.tsx` — add Calendar tab to view toggle

**Verify:** tasks appear on their due dates in week view -> switch to day view -> click event to open task.

---

### Step 18: Final Polish — Dark Mode, Performance, Accessibility

**Dark mode:**

- `next-themes` (already installed by shadcn), theme toggle in header, `ThemeProvider` in root layout

**View state in URL:**

- list/kanban/calendar + filters saved as searchParams (deep-linkable)

**Loading states:**

- `skeleton` components for task lists, milestones, comments

**Error boundaries:**

- `app/error.tsx` with `unstable_retry` (Next.js 16)

**Apply agent skills:**

- Load `vercel-react-best-practices` for performance audit
- Load `web-design-guidelines` for accessibility
- Load `vercel-composition-patterns` for component refactoring

**Instant navigation:**

- Add `unstable_instant` to static routes per Next.js 16 docs

**Verify:** dark mode toggle, deep-link views, no console errors, Lighthouse audit passes.

---

## Execution Rules

1. Each step is executed independently and fully before moving to the next.
2. Backend and frontend are interleaved per feature (not all backend then all frontend).
3. After each step: run typecheck (`tsc --noEmit`), lint (`eslint`), and verify the feature works.
4. Commit after each step is verified working.
5. Never skip verification — each step builds on the previous.